#Avea.vim
-----
You can control your avea lightbulb from inside vim ( currently only supports one bulb to contro )
You need to install: https://github.com/vfuc/avea-cli (install it globally)
###You can:
1. Add colors
2. Turn the light on and off specifying a colour
3. Set brightness
4. Delete color
5. And show list of colours
